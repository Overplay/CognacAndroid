<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="tv.ourglass.ourglasstv.Scenes.Map.MapFragment"
    android:orientation="vertical">

        <tv.ourglass.ourglasstv.Views.LightTextView
            android:id="@+id/error"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/activity_horizontal_margin"
            style="@android:style/TextAppearance.DeviceDefault"
            android:gravity="center"
            android:text="@string/noGooglePlay"
            android:visibility="gone"/>

        <com.google.android.gms.maps.MapView
            android:id="@+id/mapView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3"/>

        <ListView
            android:id="@+id/locationList"
            android:divider="@color/OGBackgroundGrey"
            android:dividerHeight=".5dp"
            android:paddingTop="@dimen/activity_vertical_margin"
            android:paddingBottom="@dimen/activity_vertical_margin"
            android:clipToPadding="false"
            android:footerDividersEnabled="false"
            android:headerDividersEnabled="false"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1.5">

        </ListView>

</LinearLayout>
